<link rel="stylesheet" href="/css/dashboard.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>📄 Contratos</h2>
        <a href="/contratos/novo" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Novo Contrato
        </a>
    </div>

    <!-- Filtros -->
    <div class="card mb-4">
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-3">
                    <label class="form-label">Cliente</label>
                    <input type="text" id="filtro-cliente" class="form-control" placeholder="Nome do cliente">
                </div>
                <div class="col-md-3">
                    <label class="form-label">Veículo</label>
                    <input type="text" id="filtro-veiculo" class="form-control" placeholder="Placa">
                </div>
                <div class="col-md-2">
                    <label class="form-label">Status</label>
                    <select id="filtro-status" class="form-select">
                        <option value="">Todos</option>
                        <option value="ativo">Ativo</option>
                        <option value="encerrado">Encerrado</option>
                        <option value="cancelado">Cancelado</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label">&nbsp;</label>
                    <button id="btn-filtrar" class="btn btn-secondary w-100">
                        <i class="bi bi-funnel"></i> Filtrar
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Tabela de Contratos -->
    <div class="card">
        <div class="card-body">
            <table id="tabela-contratos" class="table table-hover">
                <thead>
                    <tr>
                        <th>Nº Contrato</th>
                        <th>Cliente</th>
                        <th>Veículo</th>
                        <th>Vigência</th>
                        <th>Valor Mensal</th>
                        <th>Status</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Preenchido via JavaScript -->
                </tbody>
            </table>
        </div>
    </div>
</div>

<style>
    .badge-status {
        padding: 0.375rem 0.75rem;
        border-radius: 0.25rem;
        font-weight: 600;
    }

    .status-ativo {
        background-color: #28a745;
        color: white;
    }

    .status-encerrado {
        background-color: #6c757d;
        color: white;
    }

    .status-cancelado {
        background-color: #dc3545;
        color: white;
    }
</style>

<script src="/js/contratos.js"></script>